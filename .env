# Backend API URL (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:4000

# Doctor secret key — used only in doctor panel (client-side, keep separate from backend .env)
NEXT_PUBLIC_DOCTOR_KEY=1231232580
